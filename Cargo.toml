[package]
name = "bit-mail"
version = "0.0.0-dev"
edition = "2024"
license = "Apache-2.0"
description = "Local-first, Git-inspired email triage framework"
repository = "https://github.com/AFwcxx/bit-mail"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = "0.3"
